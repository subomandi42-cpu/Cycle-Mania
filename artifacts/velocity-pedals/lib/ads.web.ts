// Web stub — react-native-google-mobile-ads has no web support, so this
// file is picked by Metro on the web target via the .web.ts extension.
// The simulation path in <RewardedAdOverlay /> handles the rewarded flow.

export const TEST_BANNER_ID = "";
export const TEST_REWARDED_ID = "";

export const adsAvailable = false;

export async function initAds(): Promise<boolean> {
  return false;
}

export async function showRewardedAd(): Promise<boolean> {
  return false;
}

export const NativeBannerAd: null = null;
export const NativeBannerAdSize: null = null;
